[pytest]
pythonpath = .
testpaths = tests
markers =
    web: tests for Flask page rendering/routes
    buttons: tests for Pull Data / Update Analysis button behavior
    analysis: tests for analysis formatting and labeling
    db: tests for PostgreSQL writes and schema behavior
    integration: end-to-end tests across web + db + analysis
addopts = -q