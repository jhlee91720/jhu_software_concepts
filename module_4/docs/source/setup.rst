Setup
=====

Environment variables
---------------------

- ``DATABASE_URL``: Postgres connection string.

Run application
---------------

1. Install dependencies from ``module_4/requirements.txt``.
2. Create schema with ``python src/create_table.py``.
3. Run Flask app with ``flask --app src/flask_app.py run``.
